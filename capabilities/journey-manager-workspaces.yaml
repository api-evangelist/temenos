naftiko: 1.0.0-alpha2
info:
  label: Temenos Journey Manager API — Workspaces
  description: 'Temenos Journey Manager API — Workspaces. 1 operations. Lead operation: List Workspaces. Self-contained Naftiko
    capability covering one Temenos business surface.'
  tags:
  - Temenos
  - Workspaces
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    TEMENOS_API_KEY: TEMENOS_API_KEY
capability:
  consumes:
  - type: http
    namespace: journey-manager-workspaces
    baseUri: https://journey.temenos.com/api/v1
    description: Temenos Journey Manager API — Workspaces business capability. Self-contained, no shared references.
    resources:
    - name: workspaces
      path: /workspaces
      operations:
      - name: listworkspaces
        method: GET
        description: List Workspaces
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.TEMENOS_API_KEY}}'
  exposes:
  - type: rest
    namespace: journey-manager-workspaces-rest
    port: 8080
    description: REST adapter for Temenos Journey Manager API — Workspaces. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/workspaces
      name: workspaces
      description: REST surface for workspaces.
      operations:
      - method: GET
        name: listworkspaces
        description: List Workspaces
        call: journey-manager-workspaces.listworkspaces
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: journey-manager-workspaces-mcp
    port: 9090
    transport: http
    description: MCP adapter for Temenos Journey Manager API — Workspaces. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: list-workspaces
      description: List Workspaces
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: journey-manager-workspaces.listworkspaces
      outputParameters:
      - type: object
        mapping: $.
